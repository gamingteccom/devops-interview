stages:
  - build
  - deploy

variables:
  DOCKER_REGISTRY: CHANGE_ME
  DOCKER_REPOSITORY: CHANGE_ME
  DOCKER_TAG: CHANGE_ME
  HELM_RELEASE_NAME: CHANGE_ME

build:
  stage: build
  image: docker:28
  services:
    - docker:28-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  script:
    - # TODO
  only:
    - v2

deploy:
  stage: deploy
  image: alpine/helm:3.9.0
  script:
    - # TODO
  only:
    - v2
